/*
Problem:
Fill in your code that takes an number minutes and converts it to seconds.
Examples
toSeconds(5) -> 300
toSeconds(3) -> 180
toSeconds(2) -> 120
*/

function toSeconds(min) {
    console.log(min*60)
}
toSeconds(5);//300
toSeconds(3);//180
toSeconds(2);//120